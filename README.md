# Демо локальных моделей

## Я 💛 Фронтенд 2026

Демо‑проект: **запуск AI‑моделей прямо в браузере** (client‑side). UI построен на React + TypeScript + Vite + Ant Design.  
Цель — показать практические паттерны интеграции локальных моделей: **WebGPU/WASM**, Web Worker, прогресс загрузки, стриминг вывода, работа с кэшем.

## Запуск

В корне проекта:

```bash
npm install
npm run dev
```

Откройте в браузере адрес из консоли Vite (обычно `http://localhost:5173`).

### Production build

```bash
npm run build
npm run preview
```